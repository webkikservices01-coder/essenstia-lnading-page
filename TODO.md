# Responsive landing fixes

- [ ] Add missing responsive CSS for very small widths (<=420px) to prevent weird placement.
- [ ] Ensure hero scroll-hint doesn’t overlap on small screens; adjust bottom/size.
- [ ] Reduce hero padding and typography scaling so text fits under fixed header.
- [ ] Improve verticals section spacing and remove excessive padding on mobile.
- [ ] Add responsive fixes for contact grid and vertical padding on small screens.
- [ ] Update Header and Footer spacing if needed for small screens.
- [ ] Build and run (vite build + preview/dev) to verify.

